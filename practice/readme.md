## Practice 
<!-- 
### **HTML Practice**

1. Build a **simple form** with inputs (text, email, password, checkbox, radio button, submit).
2. Create a **profile card** using `div`, `img`, `p`, `h1`, `h2`.
3. Make a **navigation bar** with links and hover effects.
4. Structure a **blog post page** with header, article, sidebar, and footer.

---

### **CSS Layout Practice**

#### 📦 **Flexbox Exercises**

1. **Centering Content**

   * Center a box vertically and horizontally inside its container using Flexbox.

2. **Responsive Navigation Menu**

   * Create a horizontal navbar that wraps into multiple lines on smaller screens.

3. **Card Layout**

   * Design a grid of cards that are evenly spaced using `justify-content` and `align-items`.

4. **Align Items**

   * Experiment with `align-items: stretch | center | flex-start | flex-end | baseline`.

5. **Flex Grow, Shrink, and Basis**

   * Create three boxes where one grows to fill available space.

#### 📊 **Grid Exercises**

1. **Two-column Layout**

   * Create a sidebar and main content area using `display: grid` with `grid-template-columns`.
2. **Gallery Layout**

   * Build a responsive photo gallery that adjusts the number of columns based on screen size.
3. **Overlapping Items**

   * Create a layout where some elements span multiple rows and columns.

---

### **CSS Styling Practice**

1. **Buttons Styling**

   * Design buttons with hover, active, and focus states.

2. **Typography**

   * Experiment with different fonts, weights, sizes, and line heights.

3. **Animations**

   * Add a hover animation that smoothly changes background color or transforms scale.

4. **Transitions**

   * Create a card that smoothly expands when hovered over.

5. **Media Queries**

   * Build a layout that changes structure on different screen widths.

---

### **JavaScript Practice**

1. **Simple DOM Manipulation**

   * Change text or style of an element on button click.

2. **Toggle Menu**

   * Show/hide a navigation menu when a hamburger icon is clicked.

3. **Form Validation**

   * Validate email format or required fields before form submission.

4. **Event Listeners**

   * Create buttons that respond to different types of events like `mouseover`, `keypress`.

---

### **Combining Everything – Mini Projects**

1. **To-Do List App**

   * Create a task list where users can add, delete, and check tasks.

2. **Image Slider**

   * Make a simple slider where images change every few seconds or on button click.

3. **FAQ Accordion**

   * Create an expandable/collapsible FAQ section using JavaScript and CSS transitions.

4. **Portfolio Website**

   * Combine sections like About, Projects, Contact with smooth scrolling and animations.

5. **Calculator**

   * Build a basic calculator with buttons and display area using JavaScript.

---

### **Challenge Ideas**

1. Recreate popular website layouts (like YouTube, Instagram homepage).
2. Make a dark/light mode toggle.
3. Build a weather widget fetching data from an API.
4. Create interactive charts using SVG or Canvas.

--- -->
